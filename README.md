# Sri Annai Crackers — Fixed GitHub Pages Version

This version is intentionally a SINGLE `index.html` file.

It fixes:
- Add to cart
- Cart quantity +/-
- Product category filter
- Product search
- Cart total calculation
- Minimum order validation
- WhatsApp checkout

## GitHub
Replace your existing `index.html` with this one.

You can leave the old `app.js`, `products.js`, and `styles.css` files in the repository; this `index.html` does not depend on them.

After committing, refresh the website with a hard refresh:
- Chrome Android: menu ⋮ → Delete browsing data/cache if the old version remains
- Or open the site in an Incognito tab.

URL:
https://saipooja0211.github.io/Sree-Annai-Crackers/
